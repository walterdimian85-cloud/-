# 20260902 产物生成反馈日志（请据此更新硬性规则）

- 日期：2026-09-02 12:26
- 批次：20260902
- 执行者：WorkBuddy（产物生成阶段）
- 用户指示来源：用户 12:26 明确指令

## 1. Excel 单价列正文字体颜色：红色（新增规则）
- 用户要求：**三表（成交/流拍/上新）中"成交单价 / 起拍单价"正文部分字体颜色改为红色**
- 已执行：成交表 I 列（起拍单价）与 M 列（成交单价）、流拍表 I 列（起拍单价）、上新表 I 列（起拍单价）——所有数据单元格字体 = 微软雅黑 24 加粗 + **颜色 `FF0000`**（泉州 58 格、福州 125 格）
- 说明：单价列数值本身红色加粗（与 HTML 规则 §9"起拍价和单价红色加粗"对齐）
- 建议写入产物生成硬性规则 §11 Excel 部分：Excel 成交段 I/M、流拍段 I、上新段 I 单价列正文字体红色加粗（颜色 FF0000），并纳入 Excel 验收项

## 2. Excel 三表内外边框（确认已符合 v2.4 §11.12，无缺口）
- 用户反馈"今天内外边框没有加好"；核查磁盘文件：三表（表头至数据末行）**所有非合并单元格四边框均为 medium，缺口 0**（泉州 575 格、福州 1194 格确认）；合并区域（B/C/L:M）锚点四边 medium、内部无线（正常）
- 结论：磁盘文件符合规则；用户侧疑似编辑器缓存未刷新（已关闭旧实例重新打开）
- 建议：Excel 验收脚本增加"单价列红色"检查项；交付后如编辑器显示异常，先刷新/重开文件再反馈，避免误判为生成问题

## 3. 小区名称截断问题（数据准备阶段疏漏，请 codex 修复规则/清单）★核心
- 问题：20260902 唯一输入 JSON 存在 **9 条小区名称截断/错误**，但 manifest 声称"缺失 0、允许生成"，交接前验收未拦住
- 违反：交接规则 §7.4（禁止把标题/地址截断片段、行政区片段写入小区名称）、产物规则 §10.14（截断片段视为数据准备未完成，应退回数据准备阶段，生成阶段不得猜测或截取）

### 3.1 问题明细（产物生成阶段报用户 → 用户授权修正）
| 错误小区名 | 条数 | 正确值 | 城市/区域 | 地址 |
|---|---|---|---|---|
| `址在永春县五里` | 6 | **桃源北路二期安置房** | 泉州/永春 | 五里街镇桃源北路二期安置房 6-储43/44/45、4#4-1805、4#4-209、6#6-1807 |
| `花园` | 1 | **香山小镇花园一期** | 福州/罗源 | 凤山镇东外路20号香山小镇花园一期7座楼商业服务网点 |
| `政府` | 1 | **泰元花园** | 福州/平潭 | 潭城镇政府路15号泰元花园梅花1幢6D室 |
| `金园` | 1 | **金园花园** | 福州/福清 | 三山镇横坑村金园花园C区5-1号楼1306、1307单元 |

- 修正依据：地址中已完整包含可定位项目名，非猜测
- 备份：`E:\dsh\中间\诊断归档\20260902三产物输入.json.bak-小区名修正-20260902`

### 3.2 建议 codex 落实三项
1. **数据准备阶段增加截断拦截（自动检查）**：小区名长度 < 3；或含 `址在`、`在…县/区/镇` 等地址前缀片段；或以区/县/市/镇/村/路/街结尾；或为所属标准地址的前缀子串——命中即判为截断，禁止写入唯一输入
2. **补录小区名称清单**（清单路径已迁移：`E:\归档\法拍\后台数据\小区名称清单.txt`，现 947 条；以下 4 条均未收录）：
   - `桃源北路二期安置房`（泉州·永春）
   - `香山小镇花园一期`（福州·罗源；清单现仅有"香山小镇花园二期"）
   - `泰元花园`（福州·平潭）
   - `金园花园`（福州·福清）
3. **交接前验收（交接规则 §13）增加机器检查项**："小区名称不得为标题/地址截断片段"，命中即列出链接清单并判定不得生成唯一输入

## 4. 环境变更提示（供 codex 同步）
- 法拍库目录已迁移：`E:/法拍/` → `E:/归档/法拍/`（HTML 数据表在 `E:/归档/法拍/数据表html/`；小区名称清单在 `E:/归档/法拍/后台数据/小区名称清单.txt`）
- `feishu_sync.py` 内部 4 处数据源路径仍写死 `E:/法拍/数据表html/`（旧路径），当前靠复制 HTML 副本兼容；建议 codex 更新脚本路径为新目录

## 本次已执行的产物（供对照）
- Excel：泉州/福州 20260902（单价列红色、三表 medium 全边框、行高80、溢价0.00%、B/C合并、表头完整、地址直接使用输入值）
- HTML：四份（validate 4/4 PASS；9/1追加3条成交、9/2上新90条；法拍库已同步）
- 飞书：已按用户明确要求同步（上新09-02 +90、结果9/2 +68、9/1补充+3），四表新增行移到表顶
