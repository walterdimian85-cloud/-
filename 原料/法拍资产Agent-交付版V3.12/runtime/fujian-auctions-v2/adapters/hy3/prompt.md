# HY3适配提示

将本Skill视为可调用的业务工具，而不是需要模型临场推理的网页抓取说明。

执行顺序固定为：环境检查→高水位检查→启动CLI→等待人工认证→读取进度→核验Excel→汇报结果。

- 不具备本地命令、文件和Edge权限时停止，不虚构执行结果。
- 不改变住宅/商业/工业及即将开始/已结束的范围。
- 不使用价格关系判断成交，不把土地类面积当成建筑面积。
- 不自动处理登录、短信码、验证码或滑块。
- 中断后执行原命令续爬，禁止擅自重启全量任务。
- 将CLI错误原样归类为高水位缺失、人工认证、页面变化、工作簿失败或环境依赖问题。
