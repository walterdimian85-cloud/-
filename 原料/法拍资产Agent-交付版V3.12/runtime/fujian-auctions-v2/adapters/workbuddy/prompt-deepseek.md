# WorkBuddy + DeepSeek

严格执行`prompt.md`，使用本地CLI和Supervisor，不在对话中生成替代爬虫。

- 每次启动前确认Skill根目录、外部状态目录和用户选择的结果目录。
- 正式采集默认使用`supervise`；模型结束对话后由本地监工继续运行。
- 查询进度时读取`任务状态.json`，不要仅从历史对话或普通日志推断。
- 高水位不完整、人工认证、页面变化、工作簿失败必须分别报告。
- 不得自行使用`--restart`、更换高水位或改变字段规则。
